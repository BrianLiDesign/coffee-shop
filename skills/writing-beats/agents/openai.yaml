interface:
  display_name: "Writing Beats"
  short_description: "Explicitly invoke the Writing Beats workflow"
  default_prompt: "Use $writing-beats to guide this task."
policy:
  allow_implicit_invocation: false
