interface:
  display_name: "To Tickets"
  short_description: "Explicitly invoke the To Tickets workflow"
  default_prompt: "Use $to-tickets to guide this task."
policy:
  allow_implicit_invocation: false
