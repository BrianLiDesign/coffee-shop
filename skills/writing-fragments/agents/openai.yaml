interface:
  display_name: "Writing Fragments"
  short_description: "Explicitly invoke the Writing Fragments workflow"
  default_prompt: "Use $writing-fragments to guide this task."
policy:
  allow_implicit_invocation: false
