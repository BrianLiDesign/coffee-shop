# Triage Labels

The engineering skills use five canonical triage roles. These are the label strings used in this repository's GitHub issue tracker.

| Label in mattpocock/skills | Label in this repository | Meaning                                      |
| -------------------------- | ------------------------ | -------------------------------------------- |
| `needs-triage`             | `needs-triage`           | Maintainer needs to evaluate this issue      |
| `needs-info`               | `needs-info`             | Waiting on the reporter for more information |
| `ready-for-agent`          | `ready-for-agent`        | Fully specified and ready for an AFK agent   |
| `ready-for-human`          | `ready-for-human`        | Requires human implementation                |
| `wontfix`                  | `wontfix`                | Will not be actioned                         |

When a skill mentions a canonical role, use the corresponding label shown above.
